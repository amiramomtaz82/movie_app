import 'package:flutter/material.dart';

class BrowesTab extends StatelessWidget {
  const BrowesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
